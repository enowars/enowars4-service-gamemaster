version: '3'
services:
  gamemaster-checker:
    build:
      context: GamemasterChecker
      dockerfile: Dockerfile
    environment:
      - DATABASE_DOMAIN=mongodb
    ports:
      - "8000:80"
  mongodb:
    image: mongo
